import * as data from './data';
import { Header, Footer, JsonLd } from './components';
const d=data as any, site=d.site||{}, services=(d.services||[]).slice(0,5), posts=(d.blogPosts||[]).slice(0,4), stats=(d.stats||[]).slice(0,3), offer=d.staffingOffer||{};
const title=(x:any)=>typeof x==='string'?x:(x.title||x.name||x.label||x.question||'Support lane');
const text=(x:any)=>typeof x==='string'?x:(x.desc||x.excerpt||x.note||x.body||'Owned work queue with SOPs, QA, and reporting.');
const slug=(x:any)=>(x.slug||title(x).toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,''));
export default function Home(){const schema={'@context':'https://schema.org','@type':'WebSite',name:site.brand,url:`https://${site.domain}`};return <><Header/><main className="wishup"><JsonLd data={schema}/>
<section className="hero"><div className="container hero-grid"><div className="copy"><p className="speed-pill">Managed outsourcing • work queues • weekly QA</p><h1>Build an outsourcing bench that runs on clear lanes.</h1><p className="lead">For owners who need admin, support, reporting, and operations work handled without losing control of process or quality.</p><div className="actions"><a className="btn primary" href="/contact">Request outsourcing plan</a><a className="btn secondary" href="#compare">Compare options</a></div><p className="risk">Start with a pilot lane. Add responsibility after the queue is stable.</p></div><aside className="hero-form"><div className="form-head"><b>Build your lane</b><span>2-minute scope starter</span></div><label>Work to remove first</label><div className="fake-input">Admin, support, CRM, reporting…</div><label>Coverage needed</label><div className="chip-row"><span>Business hours</span><span>After hours</span></div><label>Quality check</label><div className="fake-input small">Daily notes + weekly scorecard</div><a className="btn form-btn" href="/contact">Request lane plan</a></aside></div></section>
<section className="container metric-dock">{stats.map((s:any,i:number)=><article key={i}><span>{s.label}</span><b>{s.value}</b><p>{s.note}</p></article>)}</section>
<section className="container section" id="services"><div className="section-head"><p className="eyebrow">Outsourcing lanes</p><h2>Each lane has tasks, access, owner rules, and reporting.</h2></div><div className="lane-board">{services.map((s:any,i:number)=><a href={`/services/${slug(s)}`} key={i}><div><span>lane {String(i+1).padStart(2,'0')}</span><h3>{title(s)}</h3><p>{text(s)}</p></div><b>Open lane →</b></a>)}</div></section>
<section className="compare" id="compare"><div className="container compare-grid"><div><p className="eyebrow">Why managed</p><h2>Less freelancer chaos. More operating rhythm.</h2><p>Borrowed from Wishup, Remote CoWorker, 20four7VA, and Ossisto: show the buyer exactly how work becomes a monitored queue.</p></div><div className="compare-table"><div><b>Managed plan</b><span>Screening + QA + backup + escalation</span></div><div><b>Direct freelancer</b><span>You own screening, training, access, and replacement</span></div><div><b>Loose delegation</b><span>Tasks drift without a scorecard or owner review</span></div></div></div></section>
<section className="container section resources"><div className="section-head"><p className="eyebrow">Runbooks</p><h2>Decision notes before you delegate.</h2></div><div className="runbooks">{posts.map((p:any,i:number)=><a href={`/blog/${p.slug}`} key={i}><span>{p.minutes||7} min</span><strong>{title(p)}</strong><p>{text(p)}</p></a>)}</div></section>
<section className="container final"><code>$ scope --tasks --tools --qa</code><h2>Turn messy work into a managed queue.</h2><a className="btn primary" href="/contact">Request staffing plan</a></section>
</main><Footer/></>}
