import {notFound} from 'next/navigation';import {Header,Footer} from '../../components';import {researchPosts} from '../../fleet-content';export function generateStaticParams(){return researchPosts.map(p=>({slug:p.slug}))}export default async function ResearchArticle({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const post=researchPosts.find(p=>p.slug===slug);if(!post)notFound();return <><Header/><main className="fleet-main"><article className="section article-shell"><p className="eyebrow">Philippines staffing research · {post.published}</p><h1>{post.title}</h1><p className="lead">{post.excerpt}</p><img className="article-hero-image" src={post.image.url} alt={post.image.alt}/>{post.body.map((x,i)=><p key={i}>{x}</p>)}<h2>Sources</h2><ul>{post.citations.map(source=><li key={source.url}><a href={source.url}>{source.name}</a>: {source.note}</li>)}</ul><h2>Keep planning</h2>{post.internalLinks.map(link=><p key={link.href}><a href={link.href}>{link.label}</a></p>)}<p><a className="btn primary" href={post.cta.href}>{post.cta.label}</a></p></article></main><Footer/></>}
