import {notFound} from 'next/navigation';
import {Header,Footer} from '../../components';
import {researchPosts} from '../../fleet-content';

const base='https://outsourcingassistant.com';
export function generateStaticParams(){return researchPosts.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=researchPosts.find(x=>x.slug===slug);if(!p)return {};return {title:`${p.title} | OutsourcingAssistant.com`,description:p.excerpt,alternates:{canonical:`${base}/research/${p.slug}`},openGraph:{title:p.title,description:p.excerpt,type:'article',url:`${base}/research/${p.slug}`}}}
export default async function ResearchArticle({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const post=researchPosts.find(p=>p.slug===slug);if(!post)notFound();const schema=[{'@context':'https://schema.org','@type':'Article','@id':`${base}/research/${post.slug}#article`,headline:post.title,description:post.excerpt,datePublished:post.published,dateModified:post.updated,mainEntityOfPage:`${base}/research/${post.slug}`,citation:post.sources.map(s=>s.url)}, {'@context':'https://schema.org','@type':'BreadcrumbList',itemListElement:[{'@type':'ListItem',position:1,name:'Home',item:base},{'@type':'ListItem',position:2,name:'Research',item:`${base}/research`},{'@type':'ListItem',position:3,name:post.title,item:`${base}/research/${post.slug}`}]}];return <><Header/><main className="section"><article className="container article" data-family="research"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><p className="eyebrow">{post.cluster} · Research</p><h1>{post.title}</h1><p className="lead">{post.excerpt}</p><p className="article-meta"><time dateTime={post.published}>Published {post.published}</time></p><section className="card article-block"><h2>Headline statistic</h2><p>{post.headlineStat}</p><p><b>Methodology:</b> {post.methodology}</p></section><section className="card article-block"><h2>Key stats</h2><ul className="list">{post.keyStats.map(x=><li key={x}>{x}</li>)}</ul></section><section className="card article-block"><h2>Key takeaways</h2><ul className="list">{post.takeaways.map(x=><li key={x}>{x}</li>)}</ul></section>{post.sections.map(s=><section className="card article-block" key={s.heading}><h2>{s.heading}</h2>{s.paragraphs.map(x=><p key={x}>{x}</p>)}<div className="article-table-wrap"><table className="article-table"><caption>{s.heading} evidence table</caption><thead><tr><th>Item</th><th>Finding</th><th>Source note</th></tr></thead><tbody>{s.rows.map(r=><tr key={r.label}><td>{r.label}</td><td>{r.value}</td><td>{r.source}</td></tr>)}</tbody></table></div></section>)}<section className="card article-block"><h2>Related Research</h2><div className="fleet-card-grid three">{post.related.map(slug=>{const r=researchPosts.find(x=>x.slug===slug);return r?<a className="fleet-card" href={`/research/${r.slug}`} key={r.slug}><h3>{r.title}</h3><p>{r.excerpt}</p></a>:null})}</div></section><section className="card article-block"><h2>Questions people ask</h2>{post.faqs.map(f=><div className="faq" key={f.q}><h3>{f.q}</h3><p>{f.a}</p></div>)}</section><section className="card article-block numbered-sources" aria-labelledby="sources-heading"><h2 id="sources-heading">Sources</h2><ol className="list">{post.sources.map((s,i)=><li key={s.url}><a href={s.url} rel="noreferrer">{i+1}. {s.name}</a> — {s.note}</li>)}</ol></section><p><a href="/services/research-briefing">Explore research briefing support</a> · <a href="/resources/assistant-sop-handoff-checklist">Review the SOP handoff checklist</a></p></article></main><Footer/></>}
