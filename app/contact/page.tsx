import { Header, Footer, JsonLd } from '../components';
import { site } from '../data';
export const metadata = { title: 'Contact', description: 'Request a simple assistant hiring plan with role scope, estimated hours, interview questions, and onboarding steps.', alternates: { canonical: `${site.url}/contact` } };
export default function Contact() {
 const schema = { '@context': 'https://schema.org', '@type': 'ContactPage', name: 'Request an assistant hiring plan', url: `${site.url}/contact`, publisher: { '@id': `${site.url}/#organization` }, breadcrumb: { '@type': 'BreadcrumbList', itemListElement: [{ '@type': 'ListItem', position: 1, name: 'Home', item: site.url }, { '@type': 'ListItem', position: 2, name: 'Contact', item: `${site.url}/contact` }] } };
 return <><Header/><main className="section"><JsonLd data={schema}/><div className="container two"><div><p className="eyebrow">Contact</p><h1>Request an assistant hiring plan</h1><p className="lead">Tell us what you want off your plate. We will map the role, estimated hours, questions to ask, onboarding steps, and risk controls.</p><div className="card"><h2>What helps us plan</h2><ul className="list"><li>Tasks you repeat every week</li><li>Tools the assistant would use</li><li>Hours, time zone, and language needs</li><li>Customer data, refunds, payments, or other risks</li><li>Whether you need a freelancer or managed VA service</li></ul></div><p><b>Placeholder:</b> connect this form to the approved intake endpoint or CRM before paid traffic.</p></div><form className="card" action="/thank-you"><label>Name<br/><input required name="name" /></label><label>Email<br/><input required name="email" type="email" /></label><label>What tasks do you want help with?<br/><textarea name="tasks" rows={6}/></label><label>Biggest concern<br/><select name="concern"><option>Price</option><option>Quality</option><option>Security/access</option><option>Training time</option><option>Finding the right provider</option></select></label><button className="btn" type="submit">Send request</button></form></div></main><Footer/></> }
